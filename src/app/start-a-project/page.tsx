import type { Metadata } from "next";
import { BreadcrumbSchema } from "@/components/breadcrumb-schema";
import { BOOKING_URL, EMAIL } from "@/lib/site";
export const metadata:Metadata={title:"Start a project",description:"Book a 30-minute discovery call with Mono Code to discuss your current digital presence, priorities, and whether the project is a fit.",alternates:{canonical:"/start-a-project"}};
export default function StartProjectPage(){const emailHref=`mailto:${EMAIL}?subject=${encodeURIComponent("Mono Code discovery call")}`;return <><BreadcrumbSchema items={[{name:"Home",path:"/"},{name:"Start a project",path:"/start-a-project"}]}/><section className="page-hero"><div className="shell"><p className="eyebrow">Start a project</p><h1 className="display display-small">A useful first conversation, not a hard sell.</h1><p className="lede">The discovery call is for established businesses that see a gap between the quality of their work and the experience customers find online.</p></div></section><section className="section section-white"><div className="shell"><div className="booking-panel"><div className="booking-main"><p className="eyebrow">30-minute discovery call</p><h2 className="title" style={{marginTop:"1.5rem"}}>Tell us where the current experience is falling short.</h2><p className="body-copy">We will ask about the business, the current website and local presence, who needs to choose you, and what a successful next step should achieve. You only need to provide your name and email when booking.</p>{BOOKING_URL?<a className="button button-primary" href={BOOKING_URL} target="_blank" rel="noreferrer">Open booking calendar <span className="arrow">↗</span></a>:<><p className="notice">The Google Calendar booking URL is still awaiting final setup. The email route below is active in the website and should be used until the URL is added.</p><a className="button button-primary" href={emailHref}>Email to arrange a call <span className="arrow">↗</span></a></>}<p className="body-copy" style={{fontSize:'.92rem'}}>Prefer email? Write to <a className="text-link" href={`mailto:${EMAIL}`}>{EMAIL}</a>.</p></div><aside className="booking-side"><span className="meta">What happens next</span><ol><li><p>You choose an available time or email us to arrange one.</p></li><li><p>We discuss the current situation, goals, and likely scope.</p></li><li><p>If there is a fit, we follow up with a recommendation and next step.</p></li></ol><p className="meta" style={{marginTop:"3rem"}}>Optional context</p><p>You do not need to complete a project brief. If useful, reply to the booking confirmation with your current website and the main issue you want to discuss.</p></aside></div></div></section></>}
